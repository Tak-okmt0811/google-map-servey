[project]
name = "google-map-servey"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
    "plotly>=6.9.0",
    "pydeck>=0.9.3",
    "requests>=2.34.2",
    "streamlit>=1.61.1",
]

[dependency-groups]
# exe化(Windows向け)のビルド時のみ必要。viewer実行やcollector実行には不要。
dev = [
    "pyinstaller>=6.11.0",
]
